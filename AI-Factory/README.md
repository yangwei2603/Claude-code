# AI-Factory — Read-only Reference Source

> **⚠️ 注意：此目录为只读参考源，请勿直接编辑。**
>
> 内容已同步到根目录，所有活跃开发请在根目录进行。

## 用途

此目录存放 Claude Code 工作区的原始参考文件，作为备份和历史记录保留。

## 同步状态

| 内容 | 根目录位置 | 状态 |
|------|-----------|------|
| Skills | `skills/` | ✅ 已同步 |
| Agents | `agents/` | ✅ 已同步 |
| Hooks | `.claude/hooks/` + `hooks/` | ✅ 已同步 |
| References | `references/` + `参考文件/` | ✅ 已同步 |

## 何时使用此目录

- 需要回溯历史版本时
- 需要恢复已删除的参考文件时
- 作为根目录文件的只读备份

## 注意

修改任何配置时，请编辑根目录下的文件，不要编辑此目录中的文件。
此目录的内容不会自动同步回根目录。
